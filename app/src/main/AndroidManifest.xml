<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rzgonz.nutechwallet">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".Applications"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NutechWallet"
        tools:targetApi="31">
        <activity
            android:name=".modules.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".modules.topup.TopUpActivity"
            android:exported="false" />

        <activity
            android:name=".modules.transfer.TransferActivity"
            android:exported="false" />
        <activity
            android:name=".modules.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".modules.profile.UpdateProfileActivity"
            android:exported="false" />
        <activity
            android:name=".modules.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>